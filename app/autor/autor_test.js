'use strict';

describe('myApp.autorController module', function() {

  beforeEach(module('myApp.autorController'));

  describe('autor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var autorCtrl = $controller('autorCtrl');
      expect(autorCtrl).toBeDefined();
    }));

  });
});
