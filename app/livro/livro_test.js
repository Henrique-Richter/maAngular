'use strict';

describe('myApp.livroController module', function() {

  beforeEach(module('myApp.livroController'));

  describe('livro controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var livroCtrl = $controller('livroCtrl');
      expect(livroCtrl).toBeDefined();
    }));

  });
});
